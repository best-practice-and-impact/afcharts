% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotly_theme.R
\name{theme_af_plotly}
\alias{theme_af_plotly}
\title{Analysis Function theme for plotly charts.}
\usage{
theme_af_plotly(
  chart,
  base_size = 14,
  grid = c("y", "x", "xy", "none"),
  axis = c("x", "y", "xy", "none"),
  ticks = c("xy", "x", "y", "none"),
  legend = c("right", "left", "top", "bottom", "none")
)
}
\arguments{
\item{chart}{a \code{plotly} chart object}

\item{base_size}{base font size, given in pts.}

\item{grid, }{axis, ticks 'x', 'y', 'xy' or 'none' to determine for which axes
the attribute should be drawn. Grid defaults to 'y', axis to 'x', and ticks
to 'xy'.}

\item{legend}{'right', 'left', 'top', 'bottom', or 'none' to determine the
position of the legend. Defaults to 'right'.}
}
\value{
A modified \code{plotly} chart
}
\description{
plotly theme for Analysis Function plots.
}
\examples{

library(plotly)

fig <- plot_ly(
  data = iris,
  x = ~Sepal.Length,
  y = ~Petal.Length,
  color = ~Species
) |>
  add_markers() |>
  layout(
    title = list(text = "Sepal length is positively correlated with petal length"),
    legend = list(
      title = list(text='<b>Species</b>')
    )
  )

fig |>
  theme_af_plotly()

}
